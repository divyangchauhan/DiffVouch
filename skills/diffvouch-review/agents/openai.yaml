interface:
  display_name: "DiffVouch Review"
  short_description: "Review Git diffs with a scored quality rubric"
  default_prompt: "Use $diffvouch-review in an isolated reviewer with my requested model and effort to review my current Git changes, rate them out of 5, and publish to GitHub only when I explicitly ask."
