# Evaluate the native reviewer and use published competitor results

Evaluate the DiffVouch Go CLI's own review process because this is the product the user intends to keep developing. Codex and Claude Code invocation paths and the standalone skill are outside the primary evaluation target. Compare with published competitor benchmarks rather than paying to rerun competing tools; this requires retaining historical dates and matching dataset and scoring conditions before claiming a performance difference.
